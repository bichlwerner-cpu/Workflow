# Core (free, offline-capable stickman channel)
edge-tts>=6.1.12
python-dotenv>=1.0.0
pydantic>=2.6.0
typer>=0.12.0
rich>=13.7.0
Pillow>=10.0.0

# Optional extras — install only what you use (see pyproject.toml [optional-dependencies]):
#   pip install -e ".[claude]"      # Claude-written scripts
#   pip install -e ".[elevenlabs]"  # premium TTS voice
#   pip install -e ".[captions]"    # Whisper word-perfect captions
#   pip install -e ".[footage]"     # yt-dlp footage downloads
#   pip install -e ".[upload]"      # YouTube upload
#   pip install -e ".[all]"         # everything
