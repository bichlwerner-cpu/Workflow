# character/

Hier liegt der **Charakter-Anker** für die ganze Pipeline.

- `reference.png` – das einmalig erzeugte Master-Referenzblatt von FORGE
  (Prompt in `../../image-prompts.md`, Abschnitt C). Dieses Bild wird bei **jedem** neuen
  Post als Charakter-Anker mitgegeben, damit FORGE überall gleich aussieht.

Sobald du die finale Version hast, hier als `reference.png` ablegen und nicht mehr ändern.
